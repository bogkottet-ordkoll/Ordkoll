// Copy this file to "firebase-config.js" and fill in YOUR Firebase project values.
// firebase-config.js is gitignored so your config is never committed.
// NOTE: A Firebase WEB apiKey is NOT a secret -- it ships to the browser by design.
// Real protection comes from API key restrictions + Firestore Security Rules.
window.FIREBASE_CONFIG = {
  apiKey: "YOUR_FIREBASE_WEB_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.firebasestorage.app",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
